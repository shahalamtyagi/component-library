import { Card, SecoundCard } from "./Card"
import { Layout } from "./components/Layout"

 export const CardPage= ()=>{
    return (
    <Layout>
        <Card/>
        <SecoundCard/>
    </Layout>
        
    
    )
}