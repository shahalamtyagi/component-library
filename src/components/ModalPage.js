import { Layout } from "./Layout"
import { Modal } from "./Modal"

export const ModalPage = ()=>{
    return(
        <Layout>
            <div>
                <Modal/>
            </div>
        </Layout>
    )
}