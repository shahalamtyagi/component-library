import { Card, SecoundCard } from "./Card"
import { Layout } from "./Layout"

 export const CardPage= ()=>{
    return (
    <Layout>
        <Card/>
        <SecoundCard/>
    </Layout>
        
    
    )
}